version: "3.3"

services:
  web:
    build:
      context: .
    container_name: books-api
    ports:
      - "8000:8000"
    environment:
      API_KEY: "sharad123"

    volumes:
      - ./books.db:/data/books.db   # mount your SQLite DB